# Context Map

This file indexes the project's durable knowledge. Every file under `Context/` should appear below with a one-line description of what it covers and when to use it. Treat this as the registry that keeps project context discoverable for future work.

The collaboration rules that govern how context is created and maintained live in `CLAUDE.md` (root). Slide-deck editorial conventions also currently live there and may move into `Context/` when expanded further.

## Skills

Project-specific assistant skills, reusable workflows, and skill instructions. Files live in `Context/Skills/`.

(no entries yet)

## Conventions and patterns

Editorial, layout, naming, and process conventions that should inform recurring work.

- **HTCS deck writing style.** Source: `CLAUDE.md` (section "HTCS deck writing style"). Voice, brevity rules, Java analogies for Solidity, and footer-safe layout guidance for the React slide decks under `app/src/htcs-decks/`.

## Reference

Background context, decisions, glossary, and notes worth remembering across sessions.

(no entries yet)

## Maintenance

When you add or update a file in `Context/`, add or update its entry above. Use the format `**Title.** Source: \`path\`. One-line description of what it covers.`
